name: ✅Build Status Check
run-name: "Build Status Check (Not Required)"


defaults:
  run:
    shell: pwsh


on:
  pull_request:
    paths-ignore:
      - "**.cs"
      - "**.csproj"


jobs:
  build_project:
    name: Build Project Status Check (Not Required)
    runs-on: ubuntu-latest
    steps:
    - name: Print Message
      run: 'echo "Status Check Not Required"'

  build_tests_project:
    name: Build Tests Status Check (Not Required)
    runs-on: ubuntu-latest
    steps:
    - name: Print Message
      run: 'echo "Status Check Not Required"'
